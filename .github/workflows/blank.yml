name: Win-Final-CMD

on:
  workflow_dispatch:

jobs:
  rdp-session:
    runs-on: windows-latest

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v4

    # 1. إعداد وتفعيل RDP
    - name: Enable Remote Desktop
      shell: pwsh
      run: |
        Set-ItemProperty "HKLM:\System\CurrentControlSet\Control\Terminal Server" fDenyTSConnections 0
        New-NetFirewallRule -DisplayName "Allow RDP" -Direction Inbound -LocalPort 3389 -Protocol TCP -Action Allow
        Set-ItemProperty "HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp" UserAuthentication 1
        $password = ConvertTo-SecureString "Admin@12345" -AsPlainText -Force
        Set-LocalUser -Name "runneradmin" -Password $password

    # 2. تشغيل Cloudflare (باستخدام CMD لضمان كتابة السجل)
    - name: Start Tunnel & Extract Info
      shell: pwsh
      run: |
        Write-Host "Downloading Cloudflare..."
        Invoke-WebRequest https://github.com/cloudflare/cloudflared/releases/latest/download/cloudflared-windows-amd64.exe -OutFile cloudflared.exe
        
        Write-Host "Starting Tunnel using CMD redirection..."
        
        # الحيلة هنا: استخدام cmd.exe لتشغيل البرنامج وتوجيه الأخطاء (2>) للملف
        # هذا يضمن أن الملف يكتب فوراً ولا يكون فارغاً
        $argument = "/c .\cloudflared.exe tunnel --url tcp://localhost:3389 2> tunnel.log"
        Start-Process -FilePath "cmd.exe" -ArgumentList $argument -WindowStyle Hidden

        # انتظار 10 ثواني
        Start-Sleep -Seconds 10
        
        $urlFound = $false
        $retryCount = 0
        
        # حلقة البحث (لمدة دقيقة)
        while ($retryCount -lt 60) {
            if (Test-Path "tunnel.log") {
                # قراءة الملف بطريقة تسمح بالمشاركة (تجاوز قفل الملفات)
                $stream = [System.IO.File]::Open("tunnel.log", [System.IO.FileMode]::Open, [System.IO.FileAccess]::Read, [System.IO.FileShare]::ReadWrite)
                $reader = New-Object System.IO.StreamReader($stream)
                $logContent = $reader.ReadToEnd()
                $reader.Close()
                $stream.Close()

                # البحث عن النمط tcp://url:port
                if ($logContent -match "tcp://([-a-zA-Z0-9\.]+):([0-9]+)") {
                    $hostUrl = $matches[1]
                    $portNum = $matches[2]

                    Write-Host "=============================================" -ForegroundColor Green
                    Write-Host "       SUCCESS! Tunnel is Active             " -ForegroundColor Green
                    Write-Host "=============================================" -ForegroundColor Green
                    
                    # طباعة النتائج بشكل منفصل وواضح
                    Write-Host " HOST (COMPUTER):  $hostUrl " -ForegroundColor Cyan
                    Write-Host " PORT:             $portNum " -ForegroundColor Magenta
                    Write-Host " "
                    Write-Host " USERNAME:         runneradmin " -ForegroundColor Yellow
                    Write-Host " PASSWORD:         Admin@12345 " -ForegroundColor Yellow
                    Write-Host "=============================================" -ForegroundColor Green
                    
                    $urlFound = $true
                    break
                }
            }
            
            Write-Host -NoNewline "."
            Start-Sleep -Seconds 2
            $retryCount++
        }

        if (-not $urlFound) {
            Write-Error "Failed to extract port. Dumping log file content:"
            if (Test-Path "tunnel.log") { Get-Content "tunnel.log" }
            exit 1
        }

    # 3. إبقاء الجلسة حية
    - name: Keep Session Alive
      shell: pwsh
      run: |
        Write-Host "Session is active. Press Cancel to stop."
        while ($true) {
          Start-Sleep -Seconds 60
          Write-Host -NoNewline "."
        }
