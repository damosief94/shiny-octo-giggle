name: Win-Cloudflare

on:
  workflow_dispatch:

jobs:
  rdp-session:
    runs-on: windows-latest

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v4

    # 1. إعداد وتفعيل RDP (إعدادات قوية للجدار الناري)
    - name: Enable Remote Desktop
      shell: pwsh
      run: |
        Set-ItemProperty "HKLM:\System\CurrentControlSet\Control\Terminal Server" fDenyTSConnections 0
        New-NetFirewallRule -DisplayName "Allow RDP" -Direction Inbound -LocalPort 3389 -Protocol TCP -Action Allow
        Set-ItemProperty "HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp" UserAuthentication 1
        
        $password = ConvertTo-SecureString "Admin@12345" -AsPlainText -Force
        Set-LocalUser -Name "runneradmin" -Password $password

    # 2. تحميل وتشغيل Cloudflare Tunnel
    - name: Start Cloudflare Tunnel
      shell: pwsh
      run: |
        # تحميل الأداة
        Invoke-WebRequest https://github.com/cloudflare/cloudflared/releases/latest/download/cloudflared-windows-amd64.exe -OutFile cloudflared.exe
        
        # تشغيل النفق وتوجيه المخرجات لملف log
        # Cloudflare يطبع المعلومات في StandardError وليس StandardOutput
        Start-Process -FilePath ".\cloudflared.exe" -ArgumentList "tunnel --url tcp://localhost:3389" -RedirectStandardError "cloudflared.log" -WindowStyle Hidden
        
        Write-Host "Cloudflare Tunnel starting... waiting for URL..."
        Start-Sleep -Seconds 10

    # 3. جلب وطباعة رابط الاتصال
    - name: Get Connection Info
      shell: pwsh
      run: |
        $maxRetries = 20
        $retryCount = 0
        $urlFound = $false

        while ($retryCount -lt $maxRetries) {
            if (Test-Path "cloudflared.log") {
                $logContent = Get-Content "cloudflared.log" -Raw
                # البحث عن الرابط بصيغة tcp://....trycloudflare.com:port
                if ($logContent -match "tcp://[-a-zA-Z0-9.]+\.trycloudflare\.com:[0-9]+") {
                    $rawUrl = $matches[0]
                    # إزالة tcp:// للحصول على العنوان الصافي
                    $cleanUrl = $rawUrl -replace "tcp://", ""
                    
                    Write-Host "=============================================" -ForegroundColor Green
                    Write-Host " CONNECT TO:   $cleanUrl " -ForegroundColor Yellow
                    Write-Host " USERNAME:     runneradmin " -ForegroundColor Yellow
                    Write-Host " PASSWORD:     Admin@12345 " -ForegroundColor Yellow
                    Write-Host "=============================================" -ForegroundColor Green
                    $urlFound = $true
                    break
                }
            }
            Write-Host "Waiting for URL generation... ($retryCount/$maxRetries)"
            Start-Sleep -Seconds 5
            $retryCount++
        }

        if (-not $urlFound) {
            Write-Error "Failed to find Cloudflare URL. Showing logs:"
            if (Test-Path "cloudflared.log") { Get-Content "cloudflared.log" }
        }

    # 4. إبقاء الجلسة حية
    - name: Keep Session Alive
      shell: pwsh
      run: |
        Write-Host "Session is active. Press Cancel Workflow to stop."
        while ($true) {
          Start-Sleep -Seconds 300
        }
