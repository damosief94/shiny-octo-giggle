name: Win-Final-Solution

on:
  workflow_dispatch:

jobs:
  rdp-session:
    runs-on: windows-latest

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v4

    # 1. إعداد وتفعيل RDP
    - name: Enable Remote Desktop
      shell: pwsh
      run: |
        Set-ItemProperty "HKLM:\System\CurrentControlSet\Control\Terminal Server" fDenyTSConnections 0
        New-NetFirewallRule -DisplayName "Allow RDP" -Direction Inbound -LocalPort 3389 -Protocol TCP -Action Allow
        Set-ItemProperty "HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp" UserAuthentication 1
        $password = ConvertTo-SecureString "Admin@12345" -AsPlainText -Force
        Set-LocalUser -Name "runneradmin" -Password $password

    # 2. تشغيل Cloudflare + فصل البورت برمجياً
    - name: Start Tunnel & Get Port
      shell: pwsh
      run: |
        Write-Host "Downloading Cloudflare..."
        Invoke-WebRequest https://github.com/cloudflare/cloudflared/releases/latest/download/cloudflared-windows-amd64.exe -OutFile cloudflared.exe
        
        Write-Host "Starting Cloudflare Tunnel..."
        # نشغل البرنامج ونفتح منفذ API داخلي على 45678
        $args = "tunnel --url tcp://localhost:3389 --metrics localhost:45678"
        Start-Process -FilePath ".\cloudflared.exe" -ArgumentList $args -WindowStyle Hidden

        # ننتظر 15 ثانية للتأكد من الاتصال
        Start-Sleep -Seconds 15
        
        $urlFound = $false
        $retryCount = 0
        
        # حلقة المحاولة (تتكرر حتى تجد الرابط)
        while ($retryCount -lt 60) {
            try {
                # طلب المعلومات من النظام مباشرة (ليس من ملف نصي)
                $response = Invoke-RestMethod -Uri "http://localhost:45678/quicktunnel" -ErrorAction Stop
                
                if ($response -and $response.hostname) {
                    $fullUrl = $response.hostname
                    # إزالة بادئة tcp://
                    $cleanUrl = $fullUrl -replace "tcp://", ""
                    
                    # فصل الرابط عن البورت
                    if ($cleanUrl -match ":") {
                        $parts = $cleanUrl -split ":"
                        $hostUrl = $parts[0]
                        $portNum = $parts[1]
                    } else {
                        $hostUrl = $cleanUrl
                        $portNum = "غير موجود (تأكد من نوع النفق)"
                    }

                    Write-Host "=============================================" -ForegroundColor Green
                    Write-Host "        SUCCESS! Tunnel is Active            " -ForegroundColor Green
                    Write-Host "=============================================" -ForegroundColor Green
                    
                    # طباعة المعلومات بشكل منفصل وواضح
                    Write-Host " HOST (COMPUTER):  $hostUrl " -ForegroundColor Cyan
                    Write-Host " PORT:             $portNum " -ForegroundColor Magenta
                    Write-Host " "
                    Write-Host " USERNAME:         runneradmin " -ForegroundColor Yellow
                    Write-Host " PASSWORD:         Admin@12345 " -ForegroundColor Yellow
                    Write-Host "=============================================" -ForegroundColor Green
                    
                    $urlFound = $true
                    break
                }
            }
            catch {
                # في حالة لم يعمل السيرفر بعد، ننتظر قليلاً
            }
            
            Write-Host -NoNewline "."
            Start-Sleep -Seconds 2
            $retryCount++
        }

        if (-not $urlFound) {
            Write-Error "Failed to retrieve URL from API. Cloudflared might not be running."
            exit 1
        }

    # 3. إبقاء الجلسة حية
    - name: Keep Session Alive
      shell: pwsh
      run: |
        Write-Host "Session is active. Press Cancel to stop."
        while ($true) {
          Start-Sleep -Seconds 60
          Write-Host -NoNewline "."
        }
