name: Win-Final-CF

on:
  workflow_dispatch:

jobs:
  rdp-session:
    runs-on: windows-latest

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v4

    # 1. إعداد وتفعيل RDP (نفس إعداداتك الناجحة السابقة)
    - name: Enable Remote Desktop
      shell: pwsh
      run: |
        Set-ItemProperty "HKLM:\System\CurrentControlSet\Control\Terminal Server" fDenyTSConnections 0
        New-NetFirewallRule -DisplayName "Allow RDP" -Direction Inbound -LocalPort 3389 -Protocol TCP -Action Allow
        Set-ItemProperty "HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp" UserAuthentication 1
        $password = ConvertTo-SecureString "Admin@12345" -AsPlainText -Force
        Set-LocalUser -Name "runneradmin" -Password $password

    # 2. تشغيل Cloudflare (الكود المصحح بدون أخطاء التوجيه)
    - name: Start Cloudflare Tunnel
      shell: pwsh
      run: |
        Write-Host "Downloading Cloudflare..."
        Invoke-WebRequest https://github.com/cloudflare/cloudflared/releases/latest/download/cloudflared-windows-amd64.exe -OutFile cloudflared.exe
        
        Write-Host "Starting Tunnel..."
        # هنا الحل: نوجه الأخطاء فقط لملف السجل لأن الرابط يظهر هناك
        # ونترك المخرجات العادية تذهب للفراغ لتجنب تضارب الملفات
        $process = Start-Process -FilePath ".\cloudflared.exe" -ArgumentList "tunnel --url tcp://localhost:3389" -RedirectStandardError "tunnel.log" -PassThru -WindowStyle Hidden

        # انتظار 10 ثواني لضمان إنشاء الرابط
        Start-Sleep -Seconds 10
        
        # التأكد أن البرنامج لم يتوقف
        if ($process.HasExited) {
            Write-Error "Cloudflare stopped unexpectedly!"
            if (Test-Path "tunnel.log") { Get-Content "tunnel.log" }
            exit 1
        }

        # قراءة الملف واستخراج الرابط
        $urlFound = $false
        $retryCount = 0
        
        while ($retryCount -lt 20) {
            if (Test-Path "tunnel.log") {
                # قراءة الملف بطريقة آمنة (Shared Read) لتجنب مشكلة الملف المقفول
                $stream = [System.IO.File]::Open("tunnel.log", [System.IO.FileMode]::Open, [System.IO.FileAccess]::Read, [System.IO.FileShare]::ReadWrite)
                $reader = New-Object System.IO.StreamReader($stream)
                $logContent = $reader.ReadToEnd()
                $reader.Close()
                $stream.Close()

                if ($logContent -match "tcp://[-a-zA-Z0-9.]+\.trycloudflare\.com:[0-9]+") {
                    $rawUrl = $matches[0]
                    $cleanUrl = $rawUrl -replace "tcp://", ""
                    
                    Write-Host "=============================================" -ForegroundColor Green
                    Write-Host " CONNECT TO:   $cleanUrl " -ForegroundColor Yellow
                    Write-Host " USERNAME:     runneradmin " -ForegroundColor Yellow
                    Write-Host " PASSWORD:     Admin@12345 " -ForegroundColor Yellow
                    Write-Host "=============================================" -ForegroundColor Green
                    $urlFound = $true
                    break
                }
            }
            Start-Sleep -Seconds 2
            $retryCount++
        }

        if (-not $urlFound) {
            Write-Error "Could not find URL. Logs:"
            Get-Content "tunnel.log"
            exit 1
        }

    # 3. إبقاء الجلسة حية
    - name: Keep Session Alive
      shell: pwsh
      run: |
        Write-Host "Session is running..."
        while ($true) {
          Start-Sleep -Seconds 300
        }
